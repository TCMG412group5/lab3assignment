"""
This package provides a module for parsing log files using regular expressions.

The `regex_parse` module is the only module that is importable from this package.

Example:
    >>> from my_package import regex_parse
"""
__all__ = ["regex_parse", "log_reader"]
